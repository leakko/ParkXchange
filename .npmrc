# Flat node_modules (npm-style). React Native on Windows needs a flat
# node_modules; see the comment on nodeLinker in pnpm-workspace.yaml.
node-linker=hoisted
package-import-method=copy
shamefully-hoist=true
confirm-modules-purge=false
