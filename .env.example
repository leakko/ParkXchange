# Copy to .env with `task setup`. Never commit .env.

# --- PostGIS (local docker compose) ---
POSTGRES_USER=parkxchange
POSTGRES_PASSWORD=parkxchange
POSTGRES_DB=parkxchange
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# Consumed by the Go API and the migration tooling.
DATABASE_URL=postgres://parkxchange:parkxchange@localhost:5433/parkxchange?sslmode=disable

# --- API ---
API_HOST=0.0.0.0
API_PORT=8080
API_ENV=development
LOG_LEVEL=debug

# Base64-encoded 32-byte secret used to sign access tokens.
# Generate a real one with: task auth:secret
JWT_SECRET=dev-only-insecure-secret-change-me

# --- Mobile ---
# Android emulator reaches the host loopback through 10.0.2.2.
EXPO_PUBLIC_API_URL=http://10.0.2.2:8080
EXPO_PUBLIC_WS_URL=ws://10.0.2.2:8080/v1/ws
# Public MapLibre style (street tiles; demotiles only go to zoom 6).
EXPO_PUBLIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty
# EXPO_PUBLIC_MAP_STYLE_URL=https://demotiles.maplibre.org/style.json
# EXPO_PUBLIC_MAP_STYLE_URL=pmtiles://https://your-bucket.s3.amazonaws.com/basemap.pmtiles
