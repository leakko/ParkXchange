# Physical Android device on the same Wi-Fi as this PC.
# Copy to .env.device (gitignored) via: task mobile:env:device
# Or pass an IP: task mobile:env:device -- 192.168.1.38
EXPO_PUBLIC_API_URL=http://HOST_LAN_IP:8080
EXPO_PUBLIC_WS_URL=ws://HOST_LAN_IP:8080/v1/ws
