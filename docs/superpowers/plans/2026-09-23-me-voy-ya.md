# «Me voy ya» Implementation Plan

> **For agentic workers:** Execute task-by-task. Checkbox tracking.

**Goal:** Leaving-now listings (`leaving_now`), 5/15/30 offers at guide price, map filter/banner/car icon, global `NoShowGrace` 5m.

**Architecture:** Domain flag + create/offer rules; postgres column + discovery filters; OpenAPI; mobile announce/sheet/filter/banner/layers.

**Tech Stack:** Go hexagonal API, goose, OpenAPI, Expo Mapbox.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-09-23-me-voy-ya-design.md`
- `NoShowGrace = 5m` globally
- Unreserved `leaving_now` expires at `now+60m`
- Offers: amount = price; exchange_at ∈ {now+5,15,30}m ±30s
- Cluster orange; unclustered = car icon
- Create-only (no convert)

---

### Task 1: Domain — NoShowGrace + leaving_now create/offer rules — **done**
### Task 2: Migration + postgres discovery + spots/offers use cases — **done**
### Task 3: API handlers + OpenAPI regen — **done**
### Task 4: Mobile — announce, offer chips, filter, banner, icon, i18n, NoShow copy — **done**
### Task 5: Tests, PROGRESS, commit main — **in progress**
